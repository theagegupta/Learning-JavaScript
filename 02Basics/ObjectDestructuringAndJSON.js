// destructuring

const course = {
    coursename:"Js in Hindi",
    fees:"999",
    instructor:"Hitesh",
};

const {instructor:inst} = course;
console.log(inst);

// json - a  object in which all keys  are of string type values can be of string or numeric type

